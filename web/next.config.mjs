/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i0.hdslb.com','i1.hdslb.com','i2.hdslb.com','via.placeholder.com'],
      },
};

export default nextConfig;
